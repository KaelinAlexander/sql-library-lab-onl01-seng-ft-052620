CREATE TABLE series (
  id PRIMARY KEY INTEGER,
  title TEXT,
  author TEXT,
  sub_genre TEXT
);

CREATE TABLE  (
  id PRIMARY KEY INTEGER
);

CREATE TABLE  (
  id PRIMARY KEY INTEGER
);

CREATE TABLE  (
  id PRIMARY KEY INTEGER
);

CREATE TABLE  (
  id PRIMARY KEY INTEGER
);

CREATE TABLE  (
  id PRIMARY KEY INTEGER
);